package patmat


object scratch {
  println("Welcome to the Scala worksheet")       //> Welcome to the Scala worksheet
  val list = Huffman.times(List('a', 'b', 'a'))   //> list  : List[(Char, Int)] = List((a,2), (b,1))
  val m = Huffman.makeOrderedLeafList(list)
}