
import forcomp.Anagrams._

object test {


 val w = "badca"                                  //> w  : String = badca
  val x = w.sortBy( x => x)                       //> x  : String = aabcd
  val ls = w groupBy(c => c) toList               //> ls  : List[(Char, String)] = List((b,b), (d,d), (a,aa), (c,c))
  
  ls map {case (x,y) => (x,y.length)}             //> res0: List[(Char, Int)] = List((b,1), (d,1), (a,2), (c,1))

  
  wordOccurrences(w)                              //> res1: forcomp.Anagrams.Occurrences = List((a,2), (b,1), (c,1), (d,1))
  
  wordOccurrences("abcd") == List(('a', 1), ('b', 1), ('c', 1), ('d', 1))
                                                  //> res2: Boolean = true
   wordOccurrences("Robert") == List(('b', 1), ('e', 1), ('o', 1), ('r', 2), ('t', 1))
                                                  //> res3: Boolean = true
  val zs = List("AA","BB","CC")                   //> zs  : List[String] = List(AA, BB, CC)
  zs.foldRight("")((x,y) => x + y)                //> res4: String = AABBCC
  
  "A" + "B"                                       //> res5: String("AB") = AB
  
  val y =  dictionary.map( x => (wordOccurrences(x),x))
                                                  //> y  : List[(forcomp.Anagrams.Occurrences, forcomp.Anagrams.Word)] = List((Lis
                                                  //| t((a,2), (h,1), (r,1), (s,1), (u,1)),Aarhus), (List((a,2), (n,1), (o,1), (r,
                                                  //| 1)),Aaron), (List((a,3), (b,2)),Ababa), (List((a,2), (b,1), (c,1), (k,1)),ab
                                                  //| ack), (List((a,2), (b,1), (f,1), (t,1)),abaft), (List((a,2), (b,1), (d,1), (
                                                  //| n,2), (o,1)),abandon), (List((a,2), (b,1), (d,2), (e,1), (n,2), (o,1)),aband
                                                  //| oned), (List((a,2), (b,1), (d,1), (g,1), (i,1), (n,3), (o,1)),abandoning), (
                                                  //| List((a,2), (b,1), (d,1), (e,1), (m,1), (n,3), (o,1), (t,1)),abandonment), (
                                                  //| List((a,2), (b,1), (d,1), (n,2), (o,1), (s,1)),abandons), (List((a,2), (b,1)
                                                  //| , (e,1), (s,1)),abase), (List((a,2), (b,1), (d,1), (e,1), (s,1)),abased), (L
                                                  //| ist((a,2), (b,1), (e,2), (m,1), (n,1), (s,1), (t,1)),abasement), (List((a,2)
                                                  //| , (b,1), (e,2), (m,1), (n,1), (s,2), (t,1)),abasements), (List((a,2), (b,1),
                                                  //|  (e,1), (s,2)),abases), (List((a,2), (b,1), (h,1), (s,1)),abash), (List((a,2
                                                  //| ), (b,1), (d,1), (e,1), 
                                                  //| Output exceeds cutoff limit.
 val z = y.groupBy( (x) => x._1.length)           //> z  : scala.collection.immutable.Map[Int,List[(forcomp.Anagrams.Occurrences, 
                                                  //| forcomp.Anagrams.Word)]] = Map(5 -> List((List((a,2), (h,1), (r,1), (s,1), (
                                                  //| u,1)),Aarhus), (List((a,2), (b,1), (d,1), (n,2), (o,1)),abandon), (List((a,2
                                                  //| ), (b,1), (d,1), (e,1), (s,1)),abased), (List((a,2), (b,1), (e,1), (h,1), (s
                                                  //| ,2)),abashes), (List((a,2), (b,1), (d,1), (e,1), (t,1)),abated), (List((a,2)
                                                  //| , (b,1), (e,1), (r,1), (t,1)),abater), (List((a,2), (b,1), (e,1), (s,1), (t,
                                                  //| 1)),abates), (List((a,1), (b,2), (e,1), (s,1), (y,1)),abbeys), (List((a,1), 
                                                  //| (b,2), (o,1), (s,1), (t,1)),abbots), (List((a,1), (b,1), (e,1), (s,1), (t,1)
                                                  //| ),abets), (List((a,1), (b,1), (d,1), (e,2), (t,2)),abetted), (List((a,1), (b
                                                  //| ,1), (e,2), (r,1), (t,2)),abetter), (List((a,1), (b,1), (h,1), (o,1), (r,1))
                                                  //| ,abhor), (List((a,1), (b,1), (d,1), (e,1), (i,1)),abide), (List((a,1), (b,1)
                                                  //| , (d,2), (e,1), (i,1)),abided), (List((a,2), (b,1), (g,1), (i,2), (l,1)),Abi
                                                  //| gail), (List((a,2), (b,1
                                                  //| Output exceeds cutoff limit.
   z.map{ case (x,y) => y}                        //> res6: scala.collection.immutable.Iterable[List[(forcomp.Anagrams.Occurrences
                                                  //| , forcomp.Anagrams.Word)]] = List(List((List((a,2), (h,1), (r,1), (s,1), (u,
                                                  //| 1)),Aarhus), (List((a,2), (b,1), (d,1), (n,2), (o,1)),abandon), (List((a,2),
                                                  //|  (b,1), (d,1), (e,1), (s,1)),abased), (List((a,2), (b,1), (e,1), (h,1), (s,2
                                                  //| )),abashes), (List((a,2), (b,1), (d,1), (e,1), (t,1)),abated), (List((a,2), 
                                                  //| (b,1), (e,1), (r,1), (t,1)),abater), (List((a,2), (b,1), (e,1), (s,1), (t,1)
                                                  //| ),abates), (List((a,1), (b,2), (e,1), (s,1), (y,1)),abbeys), (List((a,1), (b
                                                  //| ,2), (o,1), (s,1), (t,1)),abbots), (List((a,1), (b,1), (e,1), (s,1), (t,1)),
                                                  //| abets), (List((a,1), (b,1), (d,1), (e,2), (t,2)),abetted), (List((a,1), (b,1
                                                  //| ), (e,2), (r,1), (t,2)),abetter), (List((a,1), (b,1), (h,1), (o,1), (r,1)),a
                                                  //| bhor), (List((a,1), (b,1), (d,1), (e,1), (i,1)),abide), (List((a,1), (b,1), 
                                                  //| (d,2), (e,1), (i,1)),abided), (List((a,2), (b,1), (g,1), (i,2), (l,1)),Abiga
                                                  //| il), (List((a,2), (b,1),
                                                  //| Output exceeds cutoff limit.
}