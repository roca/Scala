import patmat.Huffman._


object scratch {
  println("Welcome to the Scala worksheet")
  val list = times(List('a', 'b', 'a','c','c','c'))
  val m = makeOrderedLeafList(list)
  
  val sampleTree = man.Leaf('x', 1), Huffman.Leaf('e', 1)),
  Huffman.Leaf('t', 2)
)
}