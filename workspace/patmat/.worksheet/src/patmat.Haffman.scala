package patmat

object Haffman {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(61); val res$0 = 
  times(List('a', 'b', 'a'));System.out.println("""res0: <error> = """ + $show(res$0))}
}
